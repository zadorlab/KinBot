    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A"    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A"    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A"    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AL    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ax    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aj    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AM    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AE    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AS    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AS    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AA    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ak    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AN    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A5    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Av    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A;    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aw    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ax    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ak    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AN    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A6    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AS    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aj    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aw    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AM    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AE    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AS    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AS    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ax    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aw    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A;    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Av    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A"    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A"    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A"    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AA    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ak    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aq    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ak    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AM    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AE    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AS    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AS    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ak    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AS    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AP    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AA    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AB    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A/    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A5    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A9    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A7    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A7    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A9    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AR    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A6    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A*    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AA    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AB    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A#    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A/    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Av    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AC    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AO    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AH    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AB    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ax    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ak    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ak    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A"    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A"    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A"    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AT    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aw    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AC    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AO    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AO    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AO    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A;    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Av    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A>    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A"    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A"    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A"    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ax    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A[    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A]    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ax    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A[    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A6    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A]    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ax    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A[    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A6    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A9    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A]    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A5    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A*    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ak    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A*    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AR    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A*    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A*    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A+    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AR    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A*    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A*    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A/    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Av    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Av    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Av    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A*    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A+    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A5    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A*    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ak    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A*    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A*    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A*    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ak    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A5    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A7    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ak    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A4    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A7    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A4    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A"    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A"    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A"    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AN    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AC    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AH    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AH    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A/    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AB    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A^    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A;    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ak    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ak    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A4    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A9    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A6    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A6    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A7    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AC    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AO    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A"    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A"    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A"    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ax    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Av    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A*    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AA    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AB    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ax    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Az    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aj    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A[    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A]    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aj    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ax    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A[    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A]    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A+    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A*    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A[    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aj    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A]    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A+    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aj    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A*    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ak    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ak    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A[    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aj    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A]    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A[    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A]    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A[    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A]    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A[    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A]    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A+    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A[    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A]    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A/    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A4    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A*    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A*    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A5    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A*    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A+    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AT    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A"    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A"    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A"    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AC    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AO    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aw    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Av    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AO    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AC    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AO    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A"    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A"    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A"    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A8    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A/    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A[    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A[    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A6    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A*    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A6    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A*    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A]    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A[    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A]    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A[    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A6    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A*    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A6    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A*    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A]    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A]    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A6    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A*    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A8    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A/    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A*    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AA    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AB    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AS    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AP    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Az    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AN    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AN    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A*    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aq    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aq    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aq    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aq    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aq    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ax    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A[    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A4    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A6    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A6    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A7    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A4    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A5    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A5    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A6    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A6    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A6    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A4    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A7    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A8    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A]    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A[    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AC    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AC    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AC    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AC    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AC    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AC    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AN    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A]    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A[    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A[    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ax    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A]    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ax    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ax    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A]    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ak    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A*    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A*    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ak    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A/    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A[    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A6    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A*    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A*    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A]    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AH    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AA    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AT    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AP    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aj    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AC    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AT    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AC    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ax    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ak    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A5    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AC    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AO    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AH    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AC    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AN    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AC    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AH    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AN    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aw    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AT    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AE    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aq    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A[    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A]    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A*    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A5    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ak    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A"    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A"    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A"    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AC    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AA    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A"    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A"    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A"    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A7    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A9    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A9    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A7    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A9    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A5    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A7    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A9    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A7    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A8    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A5    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aw    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AT    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AO    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AC    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AO    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aq    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AE    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aq    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A4    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A#    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A~    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A6    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A6    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A7    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Az    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AG    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aq    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A6    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A"    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A"    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A"    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AC    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AB    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A"    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A"    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A"    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A7    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A9    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A9    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A7    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A9    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A5    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A7    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A9    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aw    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AT    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AO    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AC    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AO    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aq    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AE    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aq    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AG    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aq    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A6    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A#    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A~    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aj    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A7    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A5    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A7    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A6    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aw    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AT    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AO    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AC    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AO    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A#    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Av    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aw    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AH    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A7    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A5    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AE    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aq    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A[    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A]    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aw    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ax    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AE    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aq    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A[    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A]    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A*    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A6    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ak    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A6    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AH    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AO    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AN    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AH    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AC    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AH    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AO    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AH    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AC    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AH    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A6    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aw    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AT    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AE    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aq    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A[    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A]    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A*    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A6    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A7    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A9    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AO    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AC    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AO    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aw    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AL    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AK    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AB    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Av    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AI    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AN    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AF    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AO    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AI    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A\    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aj    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aq    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AC    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AO    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A7    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ak    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Az    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A9    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A9    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aq    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ak    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A@    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ak    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AT    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AE    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aq    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A4    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AT    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aq    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aq    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AT    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AL    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Az    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AT    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AC    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Av    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A7    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A9    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aq    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Az    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A[    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AO    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AC    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AO    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A]    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aq    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AE    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aq    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aq    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ax    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Av    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AO    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AC    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AO    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A#    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AL    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A[    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A]    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A[    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A]    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A[    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A]    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A[    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A]    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AT    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AM    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AG    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AT    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AC    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aw    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aw    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AM    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AE    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AS    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AS    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aw    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AM    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AE    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AS    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AS    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aw    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A{    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A}    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aw    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aw    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ax    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aw    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AO    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AC    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AO    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A7    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A9    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aq    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A[    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A6    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A6    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A7    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A6    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A6    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A7    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A4    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A9    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A]    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A#    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AN    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A5    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ak    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aw    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A[    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ak    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aw    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A!    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A]    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A[    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A[    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Av    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Av    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A[    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A]    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A]    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A]    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AE    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aq    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aq    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ax    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Av    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AI    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A!    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ak    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A7    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A9    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A#    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aw    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aw    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AO    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AC    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AO    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A7    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A9    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aq    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A[    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A6    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A6    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A7    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A4    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A9    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A]    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A#    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AN    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A6    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ak    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aw    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AN    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AI    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A!    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ak    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AE    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aq    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ak    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aw    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ak    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aw    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AO    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AC    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AO    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A7    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aq    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A[    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A6    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A6    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A7    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A6    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A6    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A7    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A4    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A9    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A]    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aw    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AL    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AK    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AB    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Av    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AW    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AA    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AR    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AN    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AI    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AN    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AG    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ak    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aw    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AI    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A!    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AW    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AA    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AR    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AN    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AI    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AN    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AG    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AN    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A5    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aq    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Av    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ak    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AI    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aw    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ak    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ak    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AH    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AO    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aq    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A[    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A5    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A9    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A5    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A6    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A5    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A7    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A7    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A5    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A6    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A]    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AE    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aq    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aw    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aw    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ak    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AT    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AR    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Av    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AM    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AT    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AC    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A"    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A"    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A"    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AK    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AB    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AN    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A6    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AM    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AE    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AS    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AS    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AI    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A/    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AI    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A<    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A5    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aw    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A"    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A"    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A"    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aw    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aw    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AM    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AE    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AS    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AS    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aw    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AM    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AE    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AS    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AS    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aw    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A{    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A}    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aw    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aq    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ax    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A7    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A9    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A9    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A#    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AI    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A/    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AI    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A~    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A7    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AO    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AC    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AO    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aq    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A[    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A6    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A6    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A7    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A4    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A9    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A]    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A#    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AN    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A6    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AL    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aq    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aq    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AM    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AE    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AS    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AS    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AL    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AI    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AN    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AE    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AA    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AR    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AM    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AO    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AM    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AE    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AN    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AT    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AR    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AA    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AT    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AI    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AO    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aw    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AL    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AK    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AB    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Av    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AW    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AA    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AR    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AN    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AI    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AN    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AG    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aw    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AI    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AR    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AC    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A[    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A/    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A]    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Av    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A[    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Av    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Av    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A[    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A]    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A[    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A]    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A]    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AE    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aq    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Av    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Av    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aq    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A5    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AG    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Av    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A[    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A]    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A4    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A#    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ax    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Az    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AI    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A!    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aq    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ax    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aq    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A7    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A9    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A[    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A6    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A6    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A7    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A4    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A9    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A]    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A#    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AM    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AE    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AS    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AS    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A7    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A9    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A9    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A[    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A6    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A6    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A7    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A6    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A6    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A7    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A4    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A9    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A]    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A#    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AH    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AO    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A[    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A5    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A9    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A5    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A6    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A5    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A7    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A7    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A5    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A6    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A]    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AO    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AC    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AO    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A[    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A]    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A[    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A]    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AH    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AO    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ag    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ax    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aq    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aq    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AE    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aq    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aw    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ak    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aw    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Av    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A#    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AB    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AC    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AO    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A9    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A-    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A;    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ah    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aw    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ax    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ay    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AC    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AO    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aq    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aq    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ac    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AC    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AO    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ap    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A[    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AO    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AC    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AO    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A]    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AA    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AE    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aq    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A[    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A]    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A9    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ar    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AA    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ao    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 AE    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aq    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ab    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A[    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A2    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A]    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A3    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A9    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A,    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ad    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Al    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A0    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A1    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Af    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A=    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A_    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A'    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A:    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A     def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Au    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ae    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 As    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 At    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A.    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Am    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Aa    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 Ai    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 An    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A(    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A)    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A
    def test_rotational_constants_of_linear_co2(self):
        # exactly linear CO2: one vanishing moment, two equal ones, B = h/(8 pi^2 c) / (2 m_O r^2)
        atoms = molecule('CO2')
        symbols = atoms.get_chemical_symbols()
        r = np.linalg.norm(atoms.positions[1] - atoms.positions[0])
        expected = frequencies.ROTATIONAL_CONSTANT_FACTOR / (2 * 15.994915 * r ** 2)
        b = frequencies.rotational_constants(atoms.positions, symbols)
        self.assertAlmostEqual(b[1], expected, places=4)
        self.assertAlmostEqual(b[2], expected, places=4)
        self.assertAlmostEqual(b[1], 0.379, places=2)   # 0.390 cm-1 experimentally at r = 1.162 A